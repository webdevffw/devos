# ✅ Directory Layout

Create:

```bash
mkdir -p ~/.config/wezterm
cd ~/.config/wezterm
```

We’ll use:

```text
~/.config/wezterm/
├── wezterm.lua
├── appearance.lua
├── keys.lua
├── status.lua
├── tabs.lua
├── layout.lua
├── workspaces.lua
└── utils.lua
```

WezTerm auto-loads `wezterm.lua`.
